    def get_rates(self, symbol: str, timeframe: int = mt5.TIMEFRAME_M1, count: int = 10) -> list[dict[str, Any]]:
        rates = mt5.copy_rates_from_pos(symbol, timeframe, 0, count)

        if rates is None:
            return []

        return rates.tolist()
